@using TennisAcademyApp.ViewModels.Cart
@model IEnumerable<BagCartIndexViewModel>

@{
    ViewData["Title"] = "Your Bag Cart";
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show w-100 rounded-0 text-center mb-0" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show w-100 rounded-0 text-center mb-0" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<h2 class="text-center mb-4">Your Bag Cart</h2>

@if (!Model.Any())
{
    <div class="alert alert-info text-center">
        Your cart is empty.
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-3 g-4">
        @foreach (var bag in Model)
        {
            <div class="col">
                <div class="card h-100 shadow-sm d-flex flex-column">
                    <img src="@bag.ImageUrl" class="card-img-top object-fit-cover" alt="Bag Image" style="height: 315px;" />

                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@bag.Brand - @bag.Model</h5>
                        <p class="card-text">Price per unit: <strong>@bag.Price.ToString("C")</strong></p>
                        <p class="card-text">Quantity: @bag.Quantity</p>
                        <p class="card-text">Total: <strong>@bag.TotalPrice.ToString("C")</strong></p>

                        <form asp-controller="BagCart" asp-action="RemoveBagFromCart" method="post" class="mt-auto">
                            <input type="hidden" name="id" value="@bag.Id" />
                            <button type="submit" class="btn btn-outline-danger w-100">Remove</button>
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>

    <div class="text-center mt-4">
        <form asp-controller="BagCart" asp-action="BagCheckout" method="post">
            <button type="submit" class="btn btn-success px-5 py-2">Checkout All</button>
        </form>
    </div>
}
